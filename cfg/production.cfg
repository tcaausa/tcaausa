[buildout]
extends =
    base.cfg
    production_sources.cfg
unzip = true
parts +=
    backup
    backupcronjob
    packcronjob

eggs-directory = ${buildout:directory}/eggs
download-cache = ${buildout:directory}/downloads

develop = 
    src/tcaa.content
    src/tcaa.policy
    src/tcaa.theme
